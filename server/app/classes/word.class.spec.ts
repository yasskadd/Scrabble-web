import { Word } from './word.class';

describe('Word Class', () => {
    let word: Word;

    // CONSTRUCTOR TESTS
    it('constructor() should initialize simple attributes (is Horizontal, isFirstWord, isValid and points) correctly');
    // not sure down here
    it('constructor() should add all significant letters to wordCoordinates');
    it('constructor() should add a new letter to wordCoordinates');
    it('constructor() should add an old letter to wordCoordinates');

    // CALCULATE POINTS TESTS
    it('should add letter points correctly');
    it('should multiply word points correctly');
    it('should calculate word points correctly');
});
